// Open Graph image for the site root. Design lives in the shared module so the
// Twitter card renders the exact same artwork.
export { alt, size, contentType, default } from "./_og-assets/og-image";
